export const metadata = {
  title: 'Feeder Data Management Demo',
  description: 'Interactive map demo for feeder data management and analysis',
};

export default function DemoLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
